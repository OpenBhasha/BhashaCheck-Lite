# BhashaCheck v2 ML service. Python 3.11 recommended.
#
#   cd v2/server
#   python3.11 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt
#
# Pins mirror ml-service/requirements.txt, which documents the reasoning for
# each (numpy 2.x for pyannote 4.x / torch 2.8, whisper past the triton pin,
# huggingface-hub <1.0 for the use_auth_token alias, soundfile as torchaudio's
# I/O backend, etc.).

numpy>=2,<3

# --- Web service ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv>=1.0.1
requests==2.32.3

# --- ML ---
torch==2.8.0
torchaudio==2.8.0
soundfile==0.14.0
demucs==4.0.1              # MIT - music removal
silero-vad==5.1.2          # MIT - segmentation (also loadable via torch.hub)
pyannote.audio==4.0.7      # MIT toolkit; gated pretrained pipeline - diarization
huggingface-hub<1.0
openai-whisper==20250625   # MIT - the one fully-working local ASR provider

# Optional: Wav2Vec2 provider (WAV2VEC2_MODEL set) also needs `transformers`.
# Optional: Conformer / IndicConformer providers need requirements-nemo.txt.
